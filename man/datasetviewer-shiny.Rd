% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny.R
\name{datasetviewer-shiny}
\alias{datasetviewer-shiny}
\alias{datasetviewerOutput}
\alias{renderDatasetViewer}
\title{Shiny bindings for the dataset viewer}
\usage{
datasetviewerOutput(outputId, width = "100\%", height = "500px")

renderDatasetViewer(expr, env = parent.frame(), quoted = FALSE)
}
\arguments{
\item{outputId}{\emph{Output slot id.} \verb{<character(1)>}. Matched by
\code{datasetviewerOutput()} and \code{renderDatasetViewer()}.}

\item{width, height}{\emph{CSS sizing.} \verb{<character(1)>}. Any valid CSS size; the
grid fills the element.}

\item{expr}{\emph{Render expression.} A call that returns a \code{\link[=dataset_viewer]{dataset_viewer()}}
widget.}

\item{env, quoted}{\emph{Evaluation control.} Standard htmlwidgets render plumbing;
leave at their defaults.}
}
\value{
\emph{\code{datasetviewerOutput()}} returns a Shiny output UI element;
\emph{\code{renderDatasetViewer()}} returns a Shiny render function.
}
\description{
Output and render functions to embed \code{\link[=dataset_viewer]{dataset_viewer()}} in a Shiny app. The
widget pushes its live view state back to Shiny as inputs, so the app can
reuse the user's filter, sort, and column selection server-side.
}
\details{
\strong{Inputs published.} For an output with id \code{"viewer"} the widget sets, on
every change:
\itemize{
\item \code{input$viewer_columns} \emph{(\verb{<character>})}: selected column names.
\item \code{input$viewer_filter} \emph{(\verb{<character(1)>})}: the filter expression.
\item \code{input$viewer_sort} \emph{(\verb{<list>})}: sort keys (\code{name}, \code{dir}).
\item \code{input$viewer_view} \emph{(\verb{<character(1)>})}: \code{"names"} or \code{"labels"}.
}
}
\examples{
# ---- Example 1: read the viewer's filter and selection server-side ----
#
# The app shows a dataset and echoes the filter expression and selected
# columns the user builds in the grid. Runs only in an interactive session.
if (interactive()) {
  library(shiny)
  ui <- fluidPage(
    datasetviewerOutput("viewer", height = "500px"),
    verbatimTextOutput("state")
  )
  server <- function(input, output, session) {
    output$viewer <- renderDatasetViewer(dataset_viewer(mtcars))
    output$state <- renderText({
      paste0(
        "filter: ", input$viewer_filter, "\n",
        "columns: ", paste(input$viewer_columns, collapse = ", ")
      )
    })
  }
  shinyApp(ui, server)
}

}
